# 仅用于在 admin 目录下直接 npm run build / npm start 时生效；
# 使用 fly-print-cloud 目录下 docker-compose 构建时请修改 fly-print-cloud/.env，此处无效。

# API 路径前缀（同域部署示例）
REACT_APP_API_BASE_PATH=/api/v1
REACT_APP_AUTH_BASE_PATH=/auth

# 或使用旧版兼容：写完整 URL 时前端会只取 path 部分
# REACT_APP_API_URL=http://localhost:8012/fly-print-api
